# Hearts frontend

Run

    elm make src/HeartsFrontend.elm --output=hearts.js
	npx serve
	
Then go to http://localhost:3000

Or use `elm-watch`:

    npm install --save-dev elm-watch
	npx elm-watch hot
